{
  "name": "soundcloud-smart-preview-app",
  "version": "1.0.0",
  "private": true,
  "description": "Local SoundCloud downloader with a simple URL-based UI powered by yt-dlp.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
